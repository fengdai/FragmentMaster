apply plugin: 'com.android.application'

dependencies {
    implementation project(':fragmentmaster-appcompat')
}

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
    }
}
