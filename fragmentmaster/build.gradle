apply plugin: 'com.android.library'

dependencies {
    api 'androidx.fragment:fragment:1.0.0'
}

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
    }
}
